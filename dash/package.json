{
  "name": "aaad-dash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local dashboard over the AAAD harness: devices, runs, install matrix, screenshots.",
  "scripts": {
    "dev": "bun run src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
